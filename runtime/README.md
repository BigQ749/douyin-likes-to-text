# Runtime directory

This directory is only used locally. The application stores the selected queue, download configuration, and disposable batches here. Media and metadata are cleaned after successful processing. Everything in this directory other than this notice is excluded from Git.
